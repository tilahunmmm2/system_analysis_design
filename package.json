{
  "homepage": "https://<tilahunmmm2>.github.io/<repo-name>",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}

  "name": "system-analysis-design-site",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^4.0.0",
    "tailwindcss": "^3.3.0"
  },
  "scripts": {
  "deploy": "echo Deploying project..."
}
  "scripts": {
  "start": "node index.js",
  "test": "jest"
}
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
